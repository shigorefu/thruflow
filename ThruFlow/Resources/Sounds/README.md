# Completion sound

Add the user-provided completion sound here as `task-complete.caf`.

The app treats the sound as optional. If the file is absent or cannot be read,
task completion remains silent and all completion animations still run.
